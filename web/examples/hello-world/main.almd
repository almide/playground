// Hello, world!
//
// The smallest Almide program. `effect fn` marks a function that
// touches the outside world — printing counts. The body is just the
// expression after `=`: no braces needed for a single expression.

effect fn main() -> Unit =
  println("Hello, world!")

// Try it: change the greeting and hit Run.
// Next: ?example=pipes-and-lists
